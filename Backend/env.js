import 'dotenv/config';

export const {
  MYSQL_DATABASE,
  MYSQL_HOST,
  MYSQL_USER,
  MYSQL_PORT,
  MYSQL_PASSWORD,

  PORT,
  UPLOADS_DIR,
  JWT_SECRET,

  MAIL_TRAP_HOST,
  MAIL_TRAP_PORT,
  MAIL_TRAP_AUTH_USER,
  MAIL_TRAP_AUTH_PASS,

  ADMIN_NAME,
  ADMIN_LAST_NAME,
  ADMIN_EMAIL,
  ADMIN_PHONE,
  ADMIN_PASSWORD,
  ADMIN_ROLE,
  ADMIN_ACTIVE
} = process.env;
